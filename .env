# ===========================================================================
#  ⚠  THIS FILE IS COMMITTED TO GIT. Owner's decision, 2026-08-10.
#
#  Everything below is PUBLISHED to the repository. Anything you type here is
#  public the moment you push, and stays in the history even if the repo is
#  later made private or the line is deleted.
#
#  BEFORE PUTTING A REAL ACCOUNT HERE, do one of these first:
#      git rm --cached .env          # stop tracking it
#      echo .env >> .gitignore       # and keep it out
#  Otherwise the next `git add -A` publishes your live credentials.
#
#  These are throwaway DEMO credentials. Treat them as already compromised —
#  they were shared in a chat and are now in a public repo. Rotate the Telegram
#  token via BotFather /revoke when convenient.
# ===========================================================================

# --- MT5 terminal -----------------------------------------------------------
# Point this at the SECOND MT5 installation once it exists. Left blank the API
# picks a terminal for you, and logging in switches whichever one it picked —
# which is how mt5_beast_bot lost its symbol for an hour on 2026-08-07.
MT5_TERMINAL_PATH=C:\MT5-Gold26\terminal64.exe

MT5_LOGIN=472501540
MT5_PASSWORD=Pakistan346@
MT5_SERVER=Exness-MT5Trial16

# --- Telegram ---------------------------------------------------------------
TELEGRAM_BOT_TOKEN=8603975232:AAHov2cz7b7lV4S2DRi7Pxe2DJV2w2pQ1hM
TELEGRAM_CHAT_ID=1208668833

# --- Live trading unlocks ---------------------------------------------------
# Both are yours to set, on the real account, when you decide. I do not set
# either and no code path bypasses assert_live_unlocked().
LIVE_TRADING_ENABLED=false
LIVE_CONFIRMATION_PHRASE=
